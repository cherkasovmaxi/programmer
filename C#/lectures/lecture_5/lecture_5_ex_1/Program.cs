// Как узнать тип переменной - использовать var

using System.Linq;
using System.Xml.Linq;

var a = "la la la";
Console.WriteLine(a.GetType());

//var data = new int[] { 1, 2, 3, 4 }
//    .Where(e => e > 0)
//    .Select(e => new { q = e, w = e + 1 });
//Console.WriteLine(data.GetType());

// используйте индексы в циклах: i, j, k ,l , m;

// не надо делать много функций в функциях;